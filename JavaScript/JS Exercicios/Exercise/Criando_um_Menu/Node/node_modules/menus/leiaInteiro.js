function verificaOpcao(num, limite) {

    if (isNaN(num) || num.length == 0 || num > limite || num <= 0 || !Number.isInteger(Number(num))) {

        return true;
    
    } else {

        return false;
    }
}

module.exports = {verificaOpcao};